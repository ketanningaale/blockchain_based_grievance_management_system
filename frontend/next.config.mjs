/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["gateway.pinata.cloud", "ipfs.io", "amber-definite-reindeer-368.mypinata.cloud"],
  },
};

export default nextConfig;
